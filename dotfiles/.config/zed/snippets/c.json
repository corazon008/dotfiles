{
  "New File": {
    "prefix": "c-newfile",
    "body": [
      "#include <stdio.h>",
      "#include <stdlib.h>",
      "",
      "int main() {",
      "    // Your code here",
      "    return 0;",
      "}"
    ],
    "description": "Initialize a new C file with a basic structure."
  },
  "For Loop": {
    "prefix": "forloop",
    "body": [
      "for (int ${1:i} = 0; ${1:i} < ${2:N}; ${1:i}++) {",
      "    $0",
      "}"
    ],
    "description": "Insert a for loop."
  },
  "If Statement": {
    "prefix": "if",
    "body": [
      "if (${1:condition}) {",
      "    $0",
      "}"
    ],
    "description": "Insert an if statement."
  },
  "Function Definition": {
    "prefix": "func",
    "body": [
      "${1:void} ${2:function_name}(${3:parameters}) {",
      "    $0",
      "}"
    ]
  }
}